services:
  backend:
    develop:
      watch:
        - path: ./backend/package.json
          action: rebuild
        - path: ./backend/pnpm-lock.yaml
          action: rebuild
      
        - path: ./backend
          target: /usr/src/app
          action: sync
          ignore:
            - node_modules/
            - .pnpm-store/
